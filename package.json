{
  "name": "cka-study-site",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "description": "A Hugo study site for the Certified Kubernetes Administrator exam: decision-tree content, timed practice tasks, cloze drills, and an SM-2 spaced-repetition scheduler.",
  "scripts": {
    "build": "hugo --gc --minify --destination public",
    "serve": "hugo server --buildDrafts",
    "build:pdf": "node scripts/build-pdf.mjs",
    "build:bookmarks": "node scripts/build-bookmarks.mjs",
    "build:all": "npm run build && npm run build:pdf && npm run build:bookmarks",
    "check": "node scripts/check-data.mjs",
    "test:smoke": "node scripts/smoke.mjs"
  },
  "devDependencies": {
    "markdown-it": "^14.1.0",
    "playwright": "^1.49.0"
  }
}
