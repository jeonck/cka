---
layout: base.njk
---
<div class="hero">
  <h1>Pass the CKA by drilling decisions, not topics</h1>
  <p>{{ site.tagline }}</p>
  <p class="small">Built against CNCF curriculum <strong>{{ site.curriculumVersion }}</strong>, fetched {{ site.curriculumFetched }}.
  The exam is {{ site.examMinutes }} minutes for roughly {{ site.examTasks }} performance-based tasks at {{ site.passMark }}% to pass —
  about seven minutes each, including reading, context switching, and verifying.</p>
  <div class="weights">
    {% for d in domains %}<a href="{{ d.url }}" style="flex-grow: {{ d.weight }}" title="{{ d.title }} — {{ d.weight }}%">{{ d.weight }}%</a>{% endfor %}
  </div>
  <p class="small muted">Domain weights, to scale. Troubleshooting alone is 30% — nearly a third of the exam.</p>
  <div class="toolbar">
    <a class="btn" href="/tools/review/">Start a review session</a>
    <a class="btn ghost" href="/tools/practice/">Timed practice tasks</a>
    <a class="btn ghost" href="/tools/plan/">Build a study plan</a>
  </div>
</div>

<div class="prose">

<h2>Start here</h2>
<ol>
  <li><a href="/docs/curriculum/">Read the curriculum</a> — the exact domains and weights, transcribed from the CNCF PDF, with the date they were fetched.</li>
  <li><a href="/docs/retrospectives/">Read the retrospectives</a> — eight recurring themes from public pass/fail write-ups. Almost every reported failure is a process failure.</li>
  <li><a href="/reference/exam-strategy/">Read the strategy page</a>, then never practise untimed again.</li>
  <li><a href="/tools/plan/">Set a target date</a> and let the scheduler decide your daily workload.</li>
</ol>

<h2>The domains</h2>
<div class="grid">
{% for d in domains %}
  <a class="card" href="{{ d.url }}" style="text-decoration:none">
    <p class="eyebrow">{{ d.weight }}% of the exam</p>
    <strong>{{ d.short or d.title }}</strong>
  </a>
{% endfor %}
</div>

<h2>How the study tools work</h2>
<ul>
  <li><strong><a href="/tools/review/">Review session</a></strong> — flashcards and cloze drills through an SM-2 scheduler.
  Ease factor, interval and repetition count per item; overdue first, then new. Fully keyboard-driven.</li>
  <li><strong><a href="/tools/practice/">Practice tasks</a></strong> — scenario-based and timed, each with a model solution
  and a verification command. The timer turns red when you pass the target, because that is the moment to flag and move on.</li>
  <li><strong><a href="/tools/mnemonics/">Mnemonics</a></strong> — for what must be recalled cold: the kubeadm upgrade
  order, etcd's mandatory TLS flags, taint effects, the three conditions that make a PVC bind.</li>
  <li><strong><a href="/tools/dashboard/">Dashboard</a></strong> — per-domain mastery and time spent, sorted by exam risk
  (curriculum weight × how far you still are from mastery), so the weakest high-weight domain is always first.</li>
  <li><strong><a href="/tools/plan/">Study plan</a></strong> — target date in, daily workload out, with the last three days
  reserved for review only.</li>
</ul>

<p>Everything is stored in <code>localStorage</code>. No backend, no account, nothing leaves your browser —
so <a href="/tools/data/">export your progress</a> if you would mind losing it.</p>

<h2 id="policy">Content policy: no exam dumps</h2>
<p>The CKA is under NDA. Nothing here scrapes, reproduces, or approximates real exam questions, and no such material was
consulted while building it.</p>
<p>What the practice tasks <em>are</em>: original scenarios inferred from two public sources — the published curriculum
weights and competency list, and community retrospectives about preparation and process. If a task pattern looks
high-yield here, it is because the curriculum weights it heavily and people repeatedly report underestimating it, not
because anyone reported seeing it.</p>
<p>Community write-ups are cited by URL in the <a href="/docs/retrospectives/">retrospectives</a>, along with an honest
note about which pages could be fetched directly and which were read through search summaries.</p>

<h2>Offline</h2>
<p>The <a href="/cka-reference.pdf">one-book reference PDF</a> is generated from the same markdown and JSON as this site —
cheat sheet, every decision tree, every mnemonic, and the flags worth memorising. It regenerates on every build, so it
cannot drift from the site.</p>

</div>
